{
  "name": "papertoss",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A small, deterministic game-logic engine for a Paper Toss clone.",
  "scripts": {
    "dev": "vite",
    "build": "npm test && vite build",
    "preview": "vite preview",
    "test": "node --test"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  }
}
